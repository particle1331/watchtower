# Pinned MLflow + drivers for the self-hosted tracking/registry server.
# Exact pins are the point of self-hosting; bump deliberately.
mlflow==3.15.1
psycopg2-binary==2.9.9
azure-identity==1.17.1
azure-storage-blob==12.22.0
