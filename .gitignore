# Repo-local scratch/temp files (agents: use .tmp/ instead of system /tmp)
tmp/
.tmp/
todos/

# Build / render noise
.DS_Store
todo.txt
nb/notes/hello.qmd
.ipynb_checkpoints/
nb/notes/pdf/
nb/articles/pdf/
_site/
.jupyter/
__pycache__/
*.pyc
.pytest_cache/
.coverage
htmlcov/
.ruff_cache/
.watchtower/
.venv/
/.quarto/
**/*.quarto_ipynb

# Notebook checkpoints (actual notebook ipynbs in nb/notes/, nb/articles/, and nb/courses/)
# are tracked as canonical content).
.ipynb_checkpoints/

# Quarto single-file render intermediates
*_files/
*.aux
*.log
*.out
*.toc
*.fls
*.fdb_latexmk
*.synctex.gz
*.tex

# Résumé source + built PDF (tracked so the site can serve them)
!assets/resume.tex
!assets/resume.pdf

# Terraform local identity values (real subscription/objectId/UPN).
# Shared config lives in the committed *.tfvars; the identifying trio lives in a
# gitignored, auto-loaded *.auto.tfvars (template: *.auto.tfvars.example).
*.auto.tfvars
!*.auto.tfvars.example

# Project-local env files (written by ml-platform demo/promote.py)
projects/ml-platform/demo/.env

# BEGIN oh-my-opencode-slim worktrees
.slim/worktrees/
.slim/worktrees.json
# END oh-my-opencode-slim worktrees
*.db
